# Object Interaction System — Migration

## New adventures

1. Author `object_interaction_package.json` after Environment + World-First layers.
2. Declare `object_interaction_manifest.json`.
3. Run `python3 -m idne.object_interaction_validate`.
4. Delivery scenes reference objects — do not grant clues without interaction units.

## Legacy

Harborview/Glass Alibi not modified. Validator `SKIP`.

## Milestone 5+

Retries, false checks, full Inventory — schema fields reserved, not implemented.
